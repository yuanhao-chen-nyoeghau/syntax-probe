# Apply trained probes to c-command stimuli using Qwen/Qwen2.5-7B.
# Auto-resolves to the latest probe-training run named "qwen25_7b_ud_ewt".

runtime:
  seed: 13
  output_dir: results
  cache_dir: data/cache
  log_level: INFO

model:
  model_name: Qwen/Qwen2.5-7B
  torch_dtype: bfloat16
  batch_size: 512
  max_length: 80  # c-command stimuli (esp. RC modifier) are slightly longer than wh
  subword_pooling: mean  # must match the trained probe bank's pooling

experiment:
  kind: apply_probes
  name: c_command_qwen25_7b
  probe_run_name: qwen25_7b_ud_ewt
  stimuli_kind: c_command
  stimuli_config:
    kind: c_command_stimuli
    name: default
    num_items: 1000  # 20 stimuli per item across the three sub-experiments
  activation_cache_name: c_command_stimuli
