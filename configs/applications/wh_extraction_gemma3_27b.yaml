# Apply trained probes to wh-extraction stimuli using Gemma-3-27B.
# Auto-resolves to the latest probe-training run named "gemma3_27b_ud_ewt".

runtime:
  seed: 13
  output_dir: results
  cache_dir: data/cache
  log_level: INFO

model:
  model_name: google/gemma-3-27b-pt
  torch_dtype: bfloat16
  batch_size: 256
  max_length: 64
  subword_pooling: mean

experiment:
  kind: apply_probes
  name: wh_extraction_gemma3_27b
  probe_run_name: gemma3_27b_ud_ewt
  stimuli_kind: wh_extraction
  stimuli_config:
    kind: wh_extraction_stimuli
    name: default
    num_items: 1000
  activation_cache_name: wh_extraction_stimuli
